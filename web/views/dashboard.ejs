<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>ダッシュボード</title>
</head>
<body>
  <h1>ダッシュボード</h1>
  <p>ログイン中: <%= user.username %></p>
  <h2>参加サーバー</h2>
  <ul>
    <% guilds.forEach(guild => { %>
    <li>
      <%= guild.name %>
      <form action="/dashboard/<%= guild.id %>" method="POST">
        <input type="text" name="prefix" placeholder="Prefix" value="<%= guild.prefix || '!' %>" />
        <button type="submit">保存</button>
      </form>
    </li>
    <% }); %>
  </ul>
</body>
</html>
